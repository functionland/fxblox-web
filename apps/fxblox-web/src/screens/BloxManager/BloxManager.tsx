// STUB — replaced in place by the main-tabs screen builder (mobile: screens/BloxManager.screen.tsx).
import { paths } from '@/app/paths';
import { StubScreen } from '@/screens/StubScreen';

export default function BloxManager() {
  return <StubScreen titleKey="main.screens.bloxManager" back={paths.blox} />;
}
