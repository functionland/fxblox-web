// STUB — replaced in place by the setup screen builder (mobile: screens/InitialSetup/LinkPassword.screen.tsx).
import { SetupStubScreen } from '@/screens/StubScreen';

export default function LinkPassword() {
  return <SetupStubScreen step="linkPassword" />;
}
