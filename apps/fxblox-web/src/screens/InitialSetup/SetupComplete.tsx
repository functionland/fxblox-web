// STUB — replaced in place by the setup screen builder (mobile: screens/InitialSetup/SetupComplete.screen.tsx).
// Query params: ?manual. "Home" consumes the deep-link stash (`consumeDeepLinkStash()`), see StubScreen.
import { SetupStubScreen } from '@/screens/StubScreen';

export default function SetupComplete() {
  return <SetupStubScreen step="setupComplete" />;
}
