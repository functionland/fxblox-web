// STUB — replaced in place by the setup screen builder (mobile: screens/InitialSetup/SetBloxAuthorizer.screen.tsx).
// Query params: ?manual&ip&port&peerId (echoed by the stub).
import { SetupStubScreen } from '@/screens/StubScreen';

export default function SetBloxAuthorizer() {
  return <SetupStubScreen step="setBloxAuthorizer" />;
}
