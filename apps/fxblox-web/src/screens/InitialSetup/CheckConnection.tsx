// STUB — replaced in place by the setup screen builder (mobile: screens/InitialSetup/CheckConnection.screen.tsx).
// Query params: ?ssid (echoed by the stub).
import { SetupStubScreen } from '@/screens/StubScreen';

export default function CheckConnection() {
  return <SetupStubScreen step="checkConnection" />;
}
