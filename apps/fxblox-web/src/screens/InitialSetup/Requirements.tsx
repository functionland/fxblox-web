// STUB — replaced in place by the setup screen builder (was mobile ConnectToWallet; Chrome LNA/Bluetooth/camera explainer).
import { SetupStubScreen } from '@/screens/StubScreen';

export default function Requirements() {
  return <SetupStubScreen step="requirements" />;
}
