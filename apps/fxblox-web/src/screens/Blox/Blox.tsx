// STUB — replaced in place by the main-tabs screen builder (mobile: screens/Blox/Blox.screen.tsx).
import { StubScreen } from '@/screens/StubScreen';

export default function Blox() {
  return <StubScreen titleKey="main.screens.blox" back={false} />;
}
