// STUB — replaced in place by the main-tabs screen builder (mobile: screens/Devices.screen.tsx).
import { StubScreen } from '@/screens/StubScreen';

export default function Devices() {
  return <StubScreen titleKey="main.screens.devices" back={false} />;
}
