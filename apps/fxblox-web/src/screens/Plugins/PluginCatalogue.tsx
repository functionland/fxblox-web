// STUB — replaced in place by the main-tabs screen builder (mobile: GlobalBottomSheet plugin list → a page).
import { StubScreen } from '@/screens/StubScreen';

export default function PluginCatalogue() {
  return <StubScreen titleKey="main.screens.plugins" back={false} />;
}
