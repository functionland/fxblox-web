// STUB — replaced in place by the main-tabs screen builder (mobile: screens/Users/Users.screen.tsx, mock data).
import { StubScreen } from '@/screens/StubScreen';

export default function Users() {
  return <StubScreen titleKey="main.screens.users" back={false} />;
}
