// STUB — replaced in place by the settings screen builder (mobile: screens/Settings/Bluetooth/BluetoothCommands.screen.tsx).
// Mounted at both /settings/bluetooth (AppShell) and /setup/bluetooth (SetupShell).
import { paths } from '@/app/paths';
import { StubScreen } from '@/screens/StubScreen';

export default function BluetoothCommands() {
  return <StubScreen titleKey="settings.menu.bluetoothCommands" back={paths.settings.root} />;
}
