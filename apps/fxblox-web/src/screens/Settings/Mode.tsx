// STUB — replaced in place by the settings screen builder (mobile: screens/Settings/Mode.screen.tsx).
import { paths } from '@/app/paths';
import { StubScreen } from '@/screens/StubScreen';

export default function Mode() {
  return <StubScreen titleKey="settings.menu.mode" back={paths.settings.root} />;
}
