// STUB — replaced in place by the settings screen builder (mobile: screens/Settings/AutoPinPairing/AutoPinPairing.screen.tsx).
// Routes: /settings/autopin and the deep link /autopin-pair?token&endpoint&returnUrl (echoed by the stub).
import { paths } from '@/app/paths';
import { StubScreen } from '@/screens/StubScreen';

export default function AutoPinPairing() {
  return <StubScreen titleKey="settings.menu.autoPinPairing" back={paths.settings.root} />;
}
