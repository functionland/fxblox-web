// STUB — replaced in place by the settings screen builder (mobile: screens/Settings/About.screen.tsx).
import { paths } from '@/app/paths';
import { StubScreen } from '@/screens/StubScreen';

export default function About() {
  return <StubScreen titleKey="settings.menu.about" back={paths.settings.root} />;
}
