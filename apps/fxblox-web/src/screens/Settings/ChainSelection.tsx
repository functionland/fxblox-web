// STUB — replaced in place by the settings screen builder (mobile: screens/Settings/ChainSelection.screen.tsx).
import { paths } from '@/app/paths';
import { StubScreen } from '@/screens/StubScreen';

export default function ChainSelection() {
  return <StubScreen titleKey="settings.menu.chainSelection" back={paths.settings.root} />;
}
