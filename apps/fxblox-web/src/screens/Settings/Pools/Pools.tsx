// STUB — replaced in place by the settings screen builder (mobile: screens/Settings/Pools.screen.tsx + PoolCard).
// Also rendered as the master column by PoolsLayout at ≥ 1280px.
import { paths } from '@/app/paths';
import { StubScreen } from '@/screens/StubScreen';

export default function Pools() {
  return <StubScreen titleKey="settings.menu.pools" back={paths.settings.root} />;
}
