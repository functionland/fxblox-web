// STUB — replaced in place by the settings screen builder (mobile: screens/Settings/BloxStatusMonitor.screen.tsx).
import { paths } from '@/app/paths';
import { StubScreen } from '@/screens/StubScreen';

export default function BloxStatusMonitor() {
  return <StubScreen titleKey="settings.menu.bloxStatusMonitor" back={paths.settings.root} />;
}
