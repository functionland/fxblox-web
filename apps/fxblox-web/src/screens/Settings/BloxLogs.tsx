// STUB — replaced in place by the settings screen builder (mobile: screens/Settings/BloxLogs.screen.tsx; VITE_ENABLE_BLOX_LOGS).
import { paths } from '@/app/paths';
import { StubScreen } from '@/screens/StubScreen';

export default function BloxLogs() {
  return <StubScreen titleKey="settings.menu.bloxLogs" back={paths.settings.root} />;
}
